[package]
name = "term-mesh-cli"
version = "0.1.0"
edition = "2021"
description = "term-mesh CLI — PTY wrapper for AI agents"

[[bin]]
name = "term-mesh-run"
path = "src/main.rs"

[dependencies]
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
