[package]
name = "sync-protocol"
version = "0.1.0"
edition = "2021"
description = "Canonical wire contracts for term-mesh project sync"

[lib]
path = "src/lib.rs"

[dependencies]
blake3 = "1"
thiserror = "2"

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
