interface:
  display_name: "term-mesh Release"
  short_description: "Prepare and publish term-mesh releases from version bump to tag."
  default_prompt: "Use this skill to run the term-mesh release workflow: choose version, generate user-facing changelog updates, bump versions, open and monitor release PRs, merge, tag, and verify release artifacts."
